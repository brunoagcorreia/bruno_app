<div class="page-contents container">
  <div class="row">
    <% @orders.each do |order| %>
      <div class="orders_box col-sm-2 col-md-4">
        <h1>
          Order # <%= order.id %>:
          <%= link_to order.product.name, order %>
        </h1>
        <p><%= order.product.description %></p>
        <strong><%= number_to_currency(order.product.price.to_f, unit: "€", separator: ",") %></strong>
      </div><!-- /col -->
    <% end %>
  </div>
</div>
