<p id="notice"><%= notice %></p>

<h1>Products</h1>

<div class="container-fluid">
  <div class="row">
    <% if (@products.empty?) %>
    <p>
      Sorry, but we don't have what your are looking for.
    </p>
    <% else %>
    <%= render @products %>
    <% end %>
  </div>
  <%= link_to 'New product', new_product_path %>
</div>
